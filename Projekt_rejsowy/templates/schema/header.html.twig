<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid d-flex justify-content-between">
        <a class="navbar-brand" href="{{ path('index') }}">
            <img src="/images/logo.png" alt="main_page" class="main_logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('index') }}">Strona Główna</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">Kontakt</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('about_us') }}">O nas</a>
                </li>
                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PROVIDER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('add_voyage') }}">Dodaj Rejs</a>
                </li>   
                {% endif %}    
                {% if is_granted('ROLE_ADMIN')  %}
                <li class="nav-item">
                    <a class="nav-link" href="/RoleEdit">Zarządzaj użytkownikami</a>
                </li>   
                {% endif %}                           
            </ul>
            <ul class="navbar-nav ms-auto">

                <li class="nav-item">
                    <a class="nav-link" href="#">Twoje rezerwacje</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="/logout">Wyloguj się</a>
                </li>

                {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('register') }}">Zarejestruj się</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login">Zaloguj się</a>
                </li>
                {% endif %}
                
                
            </ul>
        </div>
    </div>
</nav>
